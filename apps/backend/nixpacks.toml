[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build", "npx prisma generate", "cp -r public dist/"]

[start]
cmd = "npx prisma db push && node dist/index.js"
