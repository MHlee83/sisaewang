[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run build && npx prisma migrate deploy && npx prisma db seed && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
